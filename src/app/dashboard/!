












<mat-sidenav-container class="sidenav-container">
    <mat-sidenav #drawer class="sidenav" fixedInViewport="true"
        [attr.role]="(isHandset$ | async) ? 'dialog' : 'navigation'" [mode]="(isHandset$ | async) ? 'over' : 'side'"
        [opened]="!(isHandset$ | async)">
        <mat-toolbar>
            <div class="logo_dash">
                <img src="assets/image/1.png" width="80"> <br>
                <img src="assets/image/2.png" width="80">
            </div>
        </mat-toolbar>

        <mat-nav-list>
          
            <a mat-list-item href='dashboard'>Dashboard </a>
            <a mat-list-item (click)="logOut()">Logout </a> 
             
        </mat-nav-list>
    </mat-sidenav>

    <mat-sidenav-content>
        <mat-toolbar color="primary">
            <button type="button" aria-label="Toggle Sidenav" mat-icon-button (click)="drawer.toggle()"
                *ngIf="isHandset$ | async">
                <mat-icon aria-label="side nav toggle icon">menu</mat-icon>
            </button>
            <div class="header_container">
                <!-- <div class="header1">{{myObj["first_name"] }} </div> -->
                <!--User type shown here> -->
                <div class="header2" style="padding-right: 13%;">{{myObj["first_name"]}} {{myObj["last_name"]}} <i class="fas fa-user-edit" [matMenuTriggerFor]="menu"></i>
                </div>
                <!--User Email shown here> -->
                 
                <!-- <div class="row" >
                    <div id="bc2" style="left: 1% !important" class="btn-group btn-breadcrumb">
                        <a (click)="home()" class="btn btn-default"><i class="fa fa-home"></i></a>
                        <a (click)="customerPage()" *ngIf="ifCustDashboard" class="btn btn-default"><div >Customer detail</div></a>
                        <a  *ngIf="ifSiteDashboard" class="btn btn-default"><div>{{warehouseName}}</div></a>
                    </div>
              </div> -->

                <mat-menu #menu="matMenu">
                    <button mat-menu-item data-toggle="modal" data-target="#resetPasswordModal">
                        <mat-icon>dialpad</mat-icon>
                        <span>Change Password </span>
                    </button>
                    
                </mat-menu>
                
            </div>
            
        </mat-toolbar>
        <!-- Add content Here -->
        
        <div [ngSwitch]="dashboardType" style="padding-top: 7% !important;">
              <div *ngSwitchCase = "'superAdmin'">
                  <app-super-admin></app-super-admin>
              </div>
              <div *ngSwitchCase = "'customer'">
                <app-customer-dashboard></app-customer-dashboard>
              </div>
              <div *ngSwitchCase = "'whouser'">
                <app-warehouse></app-warehouse>
              </div>
              <div *ngSwitchCase = "'sm'">
                <app-warehouse></app-warehouse>
              </div> 
              <div *ngSwitchCase = "'wh_metering'">
                  <app-wh-metering></app-wh-metering>
                </div> 
        </div>
              
            

            <!-- <div *ngSwitchCase=" 'customerAdmin' ">
              <app-register></app-register>
              
          </div> -->

            <!-- <div *ngSwitchCase=" 'siteManagerAdmin' ">
                  <app-register></app-register>
                  
              </div> -->

            <!-- <div *ngSwitchDefault>
                     <app-login></app-login>
                  </div> -->
        <!-- </div> -->

   </mat-sidenav-content>
</mat-sidenav-container>
<div class="modal fade" id="resetPasswordModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
  aria-hidden="true">
   <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
          <div *ngIf="!isCollapsed">
            <h5 class="modal-title" id="exampleModalLabel">Change Password</h5>
        </div>
        
        
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <div *ngIf="!isCollapsed">
        <form (ngSubmit)="onChangepwd()" #addMountForm="ngForm">
          <div class="form-group">
              <label>Old Password</label>
              <input type="text" class="form-control" name="oldPass" [(ngModel)]="changeModel.oldpassword">
          </div>
          <div class="form-group">
                <label>New Password</label>
                <input type="text" class="form-control" name="newPass" [(ngModel)]="changeModel.newpassword">
            </div>
          
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <button [disabled]="!addMountForm.form.valid" type="submit" class="btn btn-warning" style="background-color: #ff7a01; color:white;">Submit</button>
          </div>
        </form>
      </div>
       
      </div>

    </div>
  </div> 
</div>

